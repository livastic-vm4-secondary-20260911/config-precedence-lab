name: VM4 auto-timeout long workflow

on:
  pull_request:

jobs:
  long_probe:
    name: VM4 auto-timeout long
    runs-on: ubuntu-latest
    steps:
      - name: Controlled pending window
        run: sleep 180
