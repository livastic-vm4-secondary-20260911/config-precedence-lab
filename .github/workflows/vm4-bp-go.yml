name: VM4 branch protection lifecycle probe
on:
  pull_request:
    branches: [main]
permissions:
  contents: read
jobs:
  vm4-bp-go:
    name: vm4-bp-go
    runs-on: ubuntu-latest
    steps:
      - run: echo "controlled branch-protection lifecycle probe"
