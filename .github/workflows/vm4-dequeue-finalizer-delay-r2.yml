name: VM4 dequeue finalizer delay r2

on:
  pull_request:

permissions:
  contents: read

jobs:
  delay:
    name: VM4 dequeue finalizer delay r2
    runs-on: ubuntu-latest
    steps:
      - run: sleep 120
      - run: echo ok
