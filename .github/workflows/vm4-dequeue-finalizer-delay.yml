name: VM4 dequeue finalizer delay

on:
  pull_request:

permissions:
  contents: read

jobs:
  delay:
    name: VM4 dequeue finalizer delay
    runs-on: ubuntu-latest
    steps:
      - run: sleep 30
      - run: echo ok
