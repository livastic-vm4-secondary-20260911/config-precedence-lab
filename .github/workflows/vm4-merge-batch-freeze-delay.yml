name: VM4 merge-batch freeze delay

on:
  pull_request:

permissions:
  contents: read

jobs:
  delayed:
    name: VM4 merge-batch freeze delay
    runs-on: ubuntu-latest
    steps:
      - name: Hold finalizer window
        run: sleep 20
      - name: Succeed
        run: echo VM4_MERGEBATCH_FREEZE_DELAY_SUCCESS
