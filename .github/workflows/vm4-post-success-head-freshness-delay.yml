name: VM4 post-success head freshness delay

on:
  pull_request:

permissions:
  contents: read

jobs:
  delay:
    name: VM4 post-success head freshness delay
    runs-on: ubuntu-latest
    steps:
      - run: sleep 60
      - run: echo ok
