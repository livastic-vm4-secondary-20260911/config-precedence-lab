name: VM4 post-success review revocation delay

on:
  pull_request:

permissions:
  contents: read

jobs:
  delay:
    name: VM4 post-success review revocation delay
    runs-on: ubuntu-latest
    steps:
      - run: sleep 60
      - run: echo ok
