name: VM4 post-success revocation delay

on:
  pull_request:

permissions:
  contents: read

jobs:
  delay:
    name: VM4 post-success revocation delay
    runs-on: ubuntu-latest
    steps:
      - run: sleep 120
      - run: echo ok
