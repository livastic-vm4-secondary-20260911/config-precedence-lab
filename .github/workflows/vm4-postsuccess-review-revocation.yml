name: VM4 post-success review permission revocation delay
on:
  pull_request:
permissions:
  contents: read
jobs:
  delay:
    name: VM4 post-success review permission revocation delay
    runs-on: ubuntu-latest
    steps:
      - run: sleep 75
      - run: echo ok
