pull_request_rules:
  - name: VM4 direct fork squash bot-account discriminator
    conditions:
      - title = VM4 direct fork squash bot-account discriminator
    actions:
      squash:
        bot_account: livastic-vm4-primary-20260911
        commit_message: all-commits
