merge_queue:
  max_parallel_checks: 1

queue_rules:
  - name: default
    queue_conditions: []
    merge_conditions:
      - check-success = VM4 timeout late success
    checks_timeout: 1 min

merge_protections_settings:
  auto_merge_conditions:
    - title = vm4-timeout-late-success-20260916
