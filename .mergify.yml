merge_queue:
  max_parallel_checks: 1
queue_rules:
  - name: default
    queue_conditions: []
    merge_conditions:
      - check-success = vm4-never-merge-qrev
    merge_method: fast-forward
    checks_timeout: null
