merge_queue:
  mode: parallel
  max_parallel_checks: 2

scopes:
  source:
    files:
      server_scope:
        include:
          - server/**/*

queue_rules:
  - name: default
    queue_conditions: []
    merge_conditions:
      - check-success = vm4-never-merge-scope-guard
    checks_timeout: null
