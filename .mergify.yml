pull_request_rules:
  - name: vm4 branch-protection cache isolation probe
    conditions:
      - label = bug
      - check-success = vm4-never-merge-bp-cache
    actions:
      merge:
        method: merge
