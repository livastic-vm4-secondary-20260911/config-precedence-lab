pull_request_rules:
  - name: VM4 direct fork rebase bot-account discriminator
    conditions:
      - title = VM4 direct fork rebase bot-account discriminator
    actions:
      rebase:
        bot_account: livastic-vm4-primary-20260911
