pull_request_rules:
  - name: vm4 stale approval dismissal probe
    conditions:
      - label = bug
      - label = enhancement
      - label = vm4-arm-stale-review
    actions:
      merge:
        method: merge
