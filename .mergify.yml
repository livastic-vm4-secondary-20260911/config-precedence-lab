merge_protections_settings:
  reporting_method: check-runs
  post_comment: false
merge_protections:
  - name: vm4 same-sha easy policy
    if:
      - base = vm4-mp-easy-base
    success_conditions:
      - label = bug
  - name: vm4 same-sha strict policy
    if:
      - base = main
    success_conditions:
      - label = documentation
