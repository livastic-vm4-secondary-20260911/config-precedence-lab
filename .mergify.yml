pull_request_rules:
  - name: vm4 CODEOWNERS precedence probe
    conditions:
      - label = bug
      - check-success = vm4-never-merge-codeowners-precedence
    actions:
      merge:
        method: merge
