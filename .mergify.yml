queue_rules:
  - name: vm4 queue branch edit probe
    queue_conditions:
      - label = bug
    merge_conditions:
      - check-success = vm4-hold
    merge_method: fast-forward
    update_method: rebase
    max_checks_retries: 1
    allow_queue_branch_edit: false
    status_comments: all
