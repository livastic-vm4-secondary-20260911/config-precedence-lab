merge_queue:
  max_parallel_checks: 1
queue_rules:
  - name: vm4-deprecated-inplace-two-step
    queue_conditions: []
    merge_conditions:
      - check-success = vm4-never-exists
    batch_size: 1
    allow_inplace_checks: true
