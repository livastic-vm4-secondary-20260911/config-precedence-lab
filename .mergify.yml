merge_protections_settings:
  reporting_method: check-runs
  post_comment: false
merge_protections:
  - name: vm4 same-sha easy natural policy
    if:
      - base = vm4-mp-easy-natural-base
    success_conditions:
      - -draft
  - name: vm4 same-sha strict main policy
    if:
      - base = main
    success_conditions:
      - label = VM4_STRICT_LABEL_NEVER_PRESENT
