merge_queue:
  max_parallel_checks: 1

queue_rules:
  - name: default
    queue_conditions: []
    merge_conditions:
      - check-success = VM4 dequeue finalizer delay r2
    merge_method: merge-batch
    batch_size: 2
    batch_max_wait_time: 1 min
    checks_timeout: null
