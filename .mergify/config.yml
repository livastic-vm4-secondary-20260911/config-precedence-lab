merge_queue:
  max_parallel_checks: 1

queue_rules:
  - name: default
    queue_conditions: []
    merge_conditions:
      - check-success = VM4 calibration
    merge_method: merge
    checks_timeout: auto
    autoqueue: true
